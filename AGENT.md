# AGENT.md

## Project Snapshot
- Repository: `El-cmd/-Freelance-Mission-ETS-PETIT---Electricien`
- Default branch: `main`
- Detected stack: Node.js / frontend
- Notable root entries: `.github/`, `assets/`, `public/`, `scripts/`, `src/`, `.gitignore`, `components.json`, `eslint.config.js`, `index.html`, `package-lock.json`, `package.json`, `postcss.config.js`, `README.md`
- Source mix: .tsx:29, .webp:29, .ts:25, .jpg:7, .json:6, .js:3

## Working Guidelines
- Keep changes scoped to the requested behavior and follow the style already present in the touched files.
- Check `README.md`, `Makefile`, package scripts, and Docker files before introducing new commands or tooling.
- Keep package-lock.json in sync when dependencies change.
- Do not commit local secrets, `.env` files, generated dependency folders, build artifacts, or editor metadata.

## Setup
- `npm install`

## Run
- `npm run dev`

## Validate
- `npm run lint`
- `npm run build`
- `npm run preview`

## NPM Scripts Detected
- `dev`, `typecheck`, `build`, `lint`, `preview`
